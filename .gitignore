# 빌드 산출물
/ansm
/ansm.exe
*.exe
*.test

# 배포 산출물. tools/dist.ps1 이 언제든 다시 만든다.
/dist/

# 생성기가 만드는 리소스 오브젝트(cmd/ansm/rsrc_windows_*.syso)는 저장소에
# 넣는다. 평소 빌드가 `go build` 하나로 끝나게 하기 위해서다. 중간 산출물은
# 넣지 않는다.
*.res

# 로컬 Go 빌드 캐시
/.gocache/
